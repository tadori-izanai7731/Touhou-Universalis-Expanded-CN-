country_decisions = {
	THE_NIN_release_country_decision = {
		color = { 145 145 145 }
		potential = {
			ai = no
			NOT = {
				is_year = 1445
			}
			tag = SKT
			NOT = { exists = NIN }
		}
		allow = {
			always = yes
		}
		effect = {
			hidden_effect = {
				ATH = {
					every_owned_province = {
						kill_units = {
							who = owner
						}
						add_core = NIN
						remove_core = THIS
						change_religion = th_hakureishinto
					}
				}
				inherit = ATH
				every_owned_province = {
					limit = {
						is_core = NIN
					}
					remove_core = ROOT
				}
			}
			release = NIN
			hidden_effect = {
				NIN = {
					# horn_of_africa_region = {
					# 	discover_country = NIN
					# }
					# near_east_superregion = {
					# 	discover_country = NIN
					# }
					# arabian_sea_region = {
					# 	discover_country = NIN
					# }
					BYZ = {
						vassalize = NIN
					}
					THE_NIN_define_ruler_watari_nina_scripted_effect = yes
					change_government = monarchy
					add_government_reform = Gensokyan_monarchy
					change_technology_group = tech_gensokyan
					change_unit_type = tech_gensokyan
					change_primary_culture = "Youkai"
					capital_scope = {
						infantry = NIN
						infantry = NIN
						infantry = NIN
						infantry = NIN
					}
					add_treasury = 100
					add_manpower = 20
					add_adm_power = 52
					add_dip_power = 52
					add_mil_power = 52
					set_ruler_flag = touhou_character_ruler
					set_ruler_flag = touhou_original_ruler
					add_ruler_personality = immortal_personality
					change_estate_land_share = {
						estate = estate_church
						share = 20
					}
					change_estate_land_share = {
						estate = estate_nobles
						share = 20
					}
					change_estate_land_share = {
						estate = estate_burghers
						share = 20
					}
				}
			}
			switch_tag = NIN
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	THE_NIN_narrative_protagonist_set_decision = {
		color = { 242 105 217 }
		potential = {
			ai = no
			tag = NIN
			has_government_attribute = THE_NIN_fictional_goddess_reform_mechanic
		}
		allow = {
			always = yes
		}
		effect = {
			country_event = {
				id = THE_NIN_events.1
			}
		}
	}
}